# Acceptance criteria coverage

Every row resolves to Covered or Excluded. Scenario names are given as
`<capability>: <scenario title>`; all are in `specs/repo-config/spec.md`.

| Source | Requirement | Covering scenario(s) | Status |
|--------|-------------|----------------------|--------|
| AC | Plugin installed → the seeding template exists at a single documented path, and `find`ing for it succeeds | `repo-config: The template ships and is discoverable at one path` | ✅ Covered |
| AC | A repo adopting the previously hardcoded policy can start from the template's content, and the result matches the pre-issue-50 split — making `test-policy/spec.md:120` verifiable for the first time | `repo-config: A repo adopting the previous policy reproduces it from the template` | ✅ Covered |
| AC | Repo's policy file absent → the template is still not read, not quoted into any prompt, not substituted; the check still exits non-zero and the pipeline still stops | `repo-config: A missing repo policy still stops the pipeline`; `repo-config: The template cannot be reached by configuration resolution` | ✅ Covered |
| AC | Seeding still proposes a concrete policy and still writes nothing before the owner confirms; the template changes what the proposal starts from, never whether the owner confirms it | `repo-config: The owner still confirms before anything is written`; `repo-config: A repo that does not match never sees the template` | ✅ Covered — **narrowed by owner decision D2.** The criterion says the template changes "what the proposal starts from". As implemented it changes what the proposal starts from *only where the repo's shape already matches the template*; for any other shape the template is never opened. The criterion's intent — that confirmation is untouched — is met in full, and the narrowing is strictly safer. |
| AC | `ac-coverage.md:18` reflects reality rather than claiming coverage for an artifact that does not exist | — | ⚠️ Excluded — this is a correction to another change's record, not behavior this change specifies. Carried as tasks 5.2 and 5.3. A scenario asserting the content of a sibling change's coverage table would be a spec describing a document, not a capability. |
| AC | `specs/repo-config/spec.md:5` read alongside `proposal.md:12`, `test-policy/spec.md:120`, `repo-config/spec.md:111`, and `ac-coverage.md:18` → all five agree the template ships; no `MAY` left standing | — | ⚠️ Excluded — same reason. The `MAY` → `SHALL` correction is task 5.1, an edit to a pending sibling change. This change's own requirement ("The plugin ships exactly one seeding template…") is what makes the shipped-template claim true; the sibling edit only stops that change contradicting it. |
| AC | A reader of `docs/workflow.md` or `README.md` finds both the template's existence and its non-fallback status stated, and neither implies a runtime default | `repo-config: A reader learns both facts together` | ✅ Covered |
| Risk | Anchoring — a template opened first biases every seeded policy toward tiering, reintroducing the hardcoded default issue 50 removed, laundered through a confirmation prompt | `repo-config: A repo that does not match never sees the template`; `repo-config: A repo with no test suite is still first-class`; `repo-config: The header disqualifies a repo that does not match` | ✅ Covered — **contained, not eliminated.** All three mechanisms are prose read by an agent. Nothing mechanically prevents an agent from opening the template before reading the repo. Recorded in the same terms `issue-50/ac-coverage.md:27` uses for its own residual risk. |
| Risk | A template inside the plugin whose name contains the policy filename makes a single grep for that filename stop distinguishing the runtime file from the template | `repo-config: The template cannot be reached by configuration resolution` | ✅ Covered — **realised by choice, not avoided.** `TESTING.md.template` contains `TESTING.md`, so `grep -rn "TESTING\.md" plugins/spec-flow` matches both. `TESTING\.md\.template` isolates the template cleanly; isolating the runtime file takes a subtraction (`| grep -v "TESTING\.md\.template"`), since a character-class exclusion drops every mention ending a line or preceding a sentence-final period. The one-word search no longer discriminates, and nobody should read a clean result from it as evidence of anything. That was the accepted trade for a name that says what the file becomes. The no-fallback property never rested on it: `repo-config.sh` anchors every policy path at the consuming repo's root and never knows the plugin's root, so the template is unreachable by containment. Task 1.4 verifies that anchor, and it is not checked continuously. |
| Risk | Seeding notes copied into the repo's file would breach issue 50's "the seeded file reads as the repo's own editable choice" (`spec.md:164`) | `repo-config: Seeding notes never reach the repo's file` | ✅ Covered |
| Risk | The template rots — nothing runs it, nothing tests it, no repo's behavior depends on it | — | ⚠️ Excluded — inherent to encoding a policy in a file, and inherited from the decision to ship one rather than introduced by its design. Task 1.1 limits it by framing commands as per-runner examples rather than one stack's. No scenario can assert a file stays current. |
| Risk | `test-policy/spec.md:120` becomes verifiable but is not verified — nothing compares a repo's seeded file to the template | — | ⚠️ Excluded — deliberately out of scope per the issue ("no comparison of the repo's file against the template"). Recorded in `design.md` so nobody later reads the criterion as claiming the pipeline checks it. |
| Risk | Git history reads oddly: `bee37bb` asserts a template ships, and a later commit makes it true | — | ⚠️ Excluded — a property of the fix's timing, not of its design. Handled by stating it in the PR body. |
| Owner override (at the design stop) | `docs/workflow.md` states the test policy in three places (`:867`, `:889-935`, `:1032`); consolidate to one authoritative section | `repo-config: The policy is stated once, not three times` | ✅ Covered — folded in at the owner's direction; the architect recommended deferring it. **Scoped to the workflow document at review round 5**, where the triplication actually was. The requirement had reached all plugin documentation, which overclaimed: `plugins/spec-flow/README.md:66-76` restates the model before pointing, and it cannot be reduced to a bare pointer — the sibling scenario `repo-config: A reader learns both facts together` names the README and requires the template's existence and non-fallback status to be stated there. A trimmed bullet carrying just those two facts would satisfy the sibling, so the length is a front-door judgement, not a requirement. The README states which dimensions a repo must decide, not a policy for any of them. `workflow.md:678`, `:683`, and `:1047` all point rather than restate. |
| Owner override (at the design stop) | `adopt-tiering` and the template both encode the tiered split with no link; `adopt-tiering` points at the template instead | — | ⚠️ Excluded from the spec, carried as task 3.1 — **and it crosses the issue's Out section** ("re-opening the local/CI split"), so it is recorded as an explicit owner-approved expansion, not drift. Scoped to de-duplicating the wording; it does not change what `adopt-tiering` does or revisit whether tiering is right. No scenario, because no observable behavior changes. **Resolved differently:** the pointer was dropped, not added — it would have directed a policy read at the template, which this change's own requirement forbids. See task 3.1's deviation note. |
| Owner override (at review round 2) | `adopt-tiering` gates on the repo's own policy file having chosen tiering, but step 7's manual owner follow-up named branch protection alone, so a migration could land with that file still describing the layout it replaced | — | ⚠️ Excluded from the spec, carried as task 3.2 — found by the `code-review` lens after the spec was approved, and folded in at the owner's direction. Step 7 now names both follow-ups. The owner declined the larger option of softening the entry gate at `SKILL.md:11-13` into a check-and-reconcile step, so only the follow-up list changed. No scenario, because the skill's behavior is unchanged: it states a follow-up for the owner and still never writes the repo's policy file. |
| Owner override (at the design stop) | `references/` has no index and would now hold three unlike things | `repo-config: The references directory says who reads each file` | ✅ Covered — folded in at the owner's direction. |
| Owner override (at the docs polish pass) | The `spec-flow` entry in `plugins/spec-flow/.claude-plugin/marketplace.json` carries a `version` of its own, and it had drifted to `0.10.0` against `plugin.json`'s `0.37.0` | — | ⚠️ Excluded from the spec, carried as task 6.1 — found while correcting an overclaim in task 7.1's wording, and folded in at the owner's direction. A stale version in a shipped manifest is a packaging defect, not a capability this change specifies, so no scenario asserts it. The drift went uncaught because `CLAUDE.md`'s version-bump procedure names only the two `plugin.json` files; the owner declined the larger option of correcting that procedure, which governs every plugin in the repo, so it goes to the backlog instead. |
| Owner override (at review round 3) | The policy file is renamed `CI.md` → `TESTING.md`, and the shipped template to `TESTING.md.template`; exactly one filename resolves, with no alias and no acceptance of the previous name | `repo-config: One filename resolves, and only one`; `repo-config: A repo carrying only the previous filename is unconfigured`; `repo-config: The template's name names its destination` | ✅ Covered — folded in at the owner's direction after the second review pass, and specified rather than carried as a bare task, because it changes an observable contract. The file states what runs locally, what runs in CI, and what gates merge, so `CI.md` named the smaller half. **Breaking for consuming repos**, deliberately: a repo holding only `spec-flow/CI.md` reports as missing and stops, exactly as a never-configured repo does. `scripts/test-repo-config.sh` asserts that, so a fallback added later fails a test — the one property in this change with a standing check. See `overrides.md` and D1. **The template-naming half is covered only as scoped**: the scenario asserts the seeding template's own name, and the requirement is qualified to a template whose destination is a single named file. It says nothing about the workflow YAMLs in `references/ci/`, which have no single destination — the owner picks one by runner and names the copied file — so nothing verifies a naming rule for them, and none is claimed. |
| Owner override (at review round 3) | This repository's own policy claimed it has **no automated test suite** and told an agent not to look for a test runner, while four passing harnesses exist | `repo-config: The harnesses are named in the local gate`; `repo-config: Compliance still means running only what applies`; `repo-config: CI remains outside the test gate` | ✅ Covered — folded in at the owner's direction. Four harnesses run offline and pass: `plugins/spec-flow/scripts/test-repo-config.sh` (114 assertions), `plugins/spec-flow/scripts/test-board.sh` (21), `plugins/dev-skills/skills/walkthrough/scripts/test-generate-walkthrough.sh` (151), and `plugins/dev-skills/skills/ide-explain/scripts/test-generate-explain.sh` (88). The false half is replaced and the local gate now names all four, repo-wide rather than under one directory; what stays true stays: CI is not a test gate here, nothing produces a `spec-flow-failures` artifact, and an agent triggering no clause has complied in full. The claim had been misleading every agent that read it — including this change's own task 7.1, which cited it. |
| Risk | The local gate now names four harnesses by path, so moving or renaming any of them silently breaks the policy | — | ⚠️ Excluded — no scenario can assert a document stays current, the same reasoning as the template-rot row above. Bounded in practice: an agent following the gate runs a path that does not exist and sees it immediately, rather than skipping a check it believes passed. |
| Risk (found at review round 5) | A file a harness covers can sit outside that harness's own directory — `plugins/dev-skills/lib/html_shell.py` is imported by both dev-skills generators — so a covers-mapping listing only each harness's own directory sends an agent past its change to the no-harness sentence, which tells it to stop | `repo-config: The harnesses are named in the local gate` | ✅ Covered — the scenario's WHEN was broadened by task 9.3 to reach a covered file "wherever in the repository that file sits", and this is that shape one dependency level down. Both dev-skills entries now name the shared library and state that a change to it runs **both** harnesses, not one; the ide-explain entry also names the `preview-fixture/` tree its harness exercises, and the walkthrough entry its `fixtures/` tree. Found by `spec` (F1) and `test-rigor` (TR-6). Carried as tasks 10.1 and 10.2. **The residual is the template-rot residual again**: no scenario can assert a mapping stays current as imports change, so a library added later and wired into a generator can go unlisted the same way this one did. |
| Risk (found at review round 5) | `cmd_check`'s remedy told an operator to check release notes that do not exist — no `CHANGELOG`, no releases file, no git tags — while the expected path was already printed four paragraphs above, in the same output | — | ⚠️ Excluded from the spec, carried as tasks 10.3 and 10.4 — found by `observability` (OBS-4) and `code-review` (R5-1) independently, in wording task 9.5 introduced. The remedy is prose in a script's output, not a capability this change specifies, so no scenario asserts its text; `test-repo-config.sh` does, in four assertions on this block — one unchanged, one rewritten, and two added, one of them negative, so re-adding the dead-end pointer beside the working one fails. The spec'd contract around it is unchanged and still asserted: `repo-config: A repo carrying only the previous filename is unconfigured` still holds, because the new wording names only the filename the message already emits and still inspects nothing. The rebase remedy gains the same specific defusing the seeding remedy had, so neither is now defused only by a blanket sentence. |
| Risk (found at review round 5, by Fable review) | The seeding template's body asserted a build-enforced boundary and a runner-default fast command — both true for the Gradle and nextest layouts it lists, both false for the `go test -short` and `pytest -m 'not integration'` ones it also lists, so a seeder following it literally lands a policy asserting a structural guarantee the target repo does not have | — | ⚠️ Excluded from the spec, carried as task 10.8. No scenario can assert that a copied document stays true of the repo it was copied into — the same reasoning as the template-rot row. **The exposure is the worst in this change**: the file is copied verbatim into repos this project never sees, by people who never read its review. The body now states the property (a slow test cannot be selected into the fast tier) and the header names the two mechanisms that deliver it and which to write in, so the copyable text stays true for every runner and carries no seeding instruction. `go test -short` is replaced rather than rephrased: it is cooperative, so a repo relying on it fails condition 1 and should close the template — the header now says so. The per-runner examples stay, per task 1.1; one is corrected, none removed. |
| Risk (found at review round 6) | The rename warning asserted that rebasing does not help, a fact about the reader's default branch the script cannot observe — and false for the population this change creates: a branch cut before its default branch was renamed satisfies the guard but wants the rebase remedy, and renaming there buys an add/add conflict | — | ⚠️ Excluded from the spec, carried as task 11.1 — found by `code-review` (R6-1) in wording task 10.4 introduced. The remedy is prose in a script's output, so no scenario asserts its text; `test-repo-config.sh` does, now in five assertions on this block — the two new ones take one sub-case of the new condition each, and both pin the consequent rather than the condition alone, so swapping which remedy each sub-case routes to fails; the first also pins the negative clause that keeps the two conditions from overlapping. The spec'd contract is untouched: the message states the sub-cases as conditions rather than testing for them, names no filename, and inspects nothing, so `repo-config: A repo carrying only the previous filename is unconfigured` still holds. |
| Risk (found at review round 6) | The template's condition 1 stated a test — "the split is structural, the build enforces it" — that only two of the five runners it lists literally meet, and the disqualifying "close this file" line sits above the paragraph that corrected it, so a pytest or nextest seeder can disqualify itself and never read the correction | `repo-config: The header disqualifies a repo that does not match` | ✅ Covered — the scenario requires the header to name the conditions, route a failing repo to writing its own policy, and state the non-fallback status; all three still hold with condition 1 reworded to "enforced by the tooling, not by convention — the fast command cannot select a slow test". **What condition 1 requires does not change**: task 10.8's paragraph had already made "cannot be selected" the operative test, so this moves the test into the condition and removes the ordering hazard. Found by `spec` (F1), carried as task 11.2, and it reverses an earlier instruction to leave the three conditions alone, at the owner's explicit direction. |
| Risk (found at review round 6) | An exclusion list naming only scripts invites the inference that everything else in the directory is covered, so an agent editing a fixture subtree the harness never reaches gets a green run and believes its change verified | `repo-config: The harnesses are named in the local gate` | ✅ Covered — the ide-explain entry now names the fixture subtrees it does not reach and says a green run proves nothing about them, and the two fixture paths are given in full rather than as "that directory's". Found by `observability` (OBS-5) and `code-review` (R6-2), carried as tasks 11.3 and 11.5. **This one failed toward false confidence** rather than toward running nothing, which is the worse direction for a gate meant to make a change verifiable — the opposite failure from the one tasks 9.1 and 10.1 fixed. The mapping still has no mechanical check; the residual recorded on the round-five row stands. |
| Risk (found at review round 6) | "Every harness in the repo is listed here" is absolute, and `plugins/cassandra-expert/skills/training/scripts/verify-sai-capabilities.py` runs PASS/FAIL cases and exits non-zero unless all match, so on the plain reading it qualifies | `repo-config: Compliance still means running only what applies` | ✅ Covered — a sentence after the list names that directory, states that its scripts check claims against a live Cassandra 5.0+ cluster and so cannot run in this offline gate, and points at `CLAUDE.md` for when to run them. Found by `spec` (F2), carried as task 11.4. The omission was always right; what was missing was saying so where the absolute claim is made, rather than two paragraphs away. |
| Risk (found at review round 7) | The two conditions the round-six split introduced were not exclusive: a default branch mid-rename carries both filenames, matches both, and could take the rename verdict — the add/add conflict the split exists to prevent | — | ⚠️ Excluded from the spec, carried as task 12.2 — found by `observability` (OBS-6) in wording task 11.1 introduced. Excluding the new path from the first condition resolves all four states uniquely from what the reader can see, with no tree inspection: old only renames, new only rebases, both rebases (safe), neither falls through to the unconditional setup paragraph. `test-repo-config.sh` pins the negative clause, so a rewording that drops it fails; its three prose assertions now match a whitespace-collapsed copy of the output, pinning what the operator reads rather than where the line wraps, after two false failures this round came from re-flows that changed no meaning. The clean-break ruling is untouched — still no filename named, still nothing inspected. |
| Risk (found at review round 7) | The remedy block's comment still opened with the claim the round-six output fix removed, contradicting the paragraph appended six lines below it | — | ⚠️ Excluded from the spec, carried as task 12.1 — found by `code-review` (V-1). Comment-only, no behavior change, so no scenario and no assertion moves. Recorded because **the reasoning record is what the next reviewer trusts**: round six rewrote the output and the assertion block's comment, and appended to this one without deleting what it contradicted, leaving the stale half to read first. This is the third round in which a sentence about this block outlived the behavior it described. |
| Risk (pre-existing, found at review round 7) | `test-repo-config.sh:475-481` is flaky: two `seed-config.sh` fail-mode pushes create two remote branches named with `$(date +%Y%m%d%H%M%S)-$$`, and the assertion picks lexicographically with `for-each-ref \| head -n 1` while `fail_out` came from the first, so same-second PIDs straddling a digit-width boundary select the wrong branch | — | ⚠️ Excluded — **pre-existing on `origin/main`, not introduced by this change**, and outside its scope. Found by `observability`; seen once at 111/3 against twice at 114/0. Deliberately left for the owner's backlog rather than widening a branch seven rounds deep. Recorded here so a future red run on those three `expect_reports_recovery` assertions is recognised as this flake and re-run, not chased as a regression. |
