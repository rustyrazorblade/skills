| Source | Requirement | Covering scenario(s) | Status |
|--------|-------------|----------------------|--------|
| AC | WHEN a repo has only review-tools installed, THEN `/tech-debt` is available and runs the full repo-wide structural audit exactly as it does today under spec-flow | `tech-debt: Standalone repo-wide structural audit` — "review-tools installed alone" | ✅ Covered |
| AC | WHEN both plugins are installed, THEN spec-flow's project-manager cadence recommendation and docs/workflow.md reference the review-tools skill, not a duplicated copy | `tech-debt: spec-flow's own references point at the moved skill` — "Both plugins installed"; `tech-debt: Cadence recommendation degrades gracefully...` — "spec-flow and review-tools both installed" | ✅ Covered |
| AC | WHEN spec-flow is installed WITHOUT review-tools, THEN project-manager degrades gracefully — states the feature needs review-tools installed, does not error or reference a missing skill | `tech-debt: Cadence recommendation degrades gracefully without an explicit availability probe` — "spec-flow installed without review-tools" | ✅ Covered |
| AC | WHEN the move is complete, THEN no duplicate copy of the skill remains under plugins/spec-flow/ | `tech-debt: Single, non-duplicated home for the skill` — "No duplicate remains" | ✅ Covered |
| Design | Owner decision: the moved SKILL.md carries no spec-flow mention at all (overrides architect's one-paragraph-integration recommendation) | `tech-debt: Skill instructions carry no dependency on another plugin` — "Read in isolation" | ✅ Covered |
| Design | Owner decision: no explicit `claude plugin list`/jq detection command anywhere; conditional prose instead, trusting the agent's own runtime skill awareness | `tech-debt: Cadence recommendation degrades gracefully without an explicit availability probe` (requirement text explicitly rules out an explicit probe) | ✅ Covered |
| Design | Owner decision: command renamed to `/tech-debt`, no compatibility alias kept | `tech-debt: Command renamed, no alias` — "Old command no longer resolves" | ✅ Covered |
| Risk | Architect-flagged: the filed-issue `## Direction` section heading must survive the move verbatim — spec-flow's fast path gates on it being present | — | ⚠️ Excluded — a preservation constraint on existing, unchanged behavior (the issue-filing template in step 6), not new behavior this change introduces; enforced at the task level (`tasks.md` 1.1–1.2 keep the template itself untouched, only trimming surrounding provenance prose) rather than as a new spec scenario |
| Risk | Architect-flagged: duplicated plugin-detection logic across call sites if a new inline jq check were added here | — | ⚠️ Excluded — moot: the owner's decision 2 removes the only new detection call site entirely, so this change introduces no new duplication |
