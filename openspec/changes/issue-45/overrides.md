## Overrides existing behavior

None — this change only adds a new `tech-debt` capability. The skill had no formal OpenSpec spec
while it lived under spec-flow (`openspec/specs/` contains only `explain` today), so there is no
baseline `tech-debt` requirement to override. This change's delta spec contains no `MODIFIED
Requirements` or `REMOVED Requirements` sections against any existing baseline capability.

## Conflicts with other in-flight changes

Two other open change directories exist: `issue-42` and `openspec-aware-explain-view`.

- `issue-42` touches only the `walkthrough` capability (`openspec/changes/issue-42/specs/
  walkthrough/spec.md`) — no overlap with `tech-debt`.
- `openspec-aware-explain-view` touches only the `explain` capability (`openspec/changes/
  openspec-aware-explain-view/specs/explain/spec.md`) — no overlap with `tech-debt`.

None found.
