# AC → scenario coverage for `issue-50`

Every acceptance criterion from the issue, plus every risk the architect surfaced. Each row resolves to Covered or Excluded-with-reason.

| Source | Requirement | Covering scenario(s) | Status |
|--------|-------------|----------------------|--------|
| AC | `CI.md` exists + `implement` runs → test instruction derived from that file; plugin defaults appear nowhere unless the repo names them | `test-policy: The instruction is derived from the repo's file`; `test-policy: No plugin default can leak into a prompt` | ✅ Covered |
| AC | Repo's `CI.md` says full suite local + CI as merge gate only → teammates run full suite locally; no avoid-full-suite instruction anywhere, including draft PR body and final report | `test-policy: The policy is obeyed as written, whichever way it points` | ✅ Covered |
| AC | `SPEC_FLOW_CONFIG_DIR` set → read `<dir>/CI.md`; unset → `spec-flow/CI.md` at repo root | `repo-config: Default location`; `repo-config: Relocated by environment` | ✅ Covered |
| AC | All required config present → script exits 0 and prints nothing | `repo-config: Everything present costs nothing` | ✅ Covered |
| AC | Missing or unusable → exits non-zero, prints each missing path plus the command that creates it; script owns the message, no caller restates | `repo-config: Something missing produces the complete message`; `repo-config: Unusable is more than absent`; `repo-config: A skill stops before doing any work` | ✅ Covered |
| AC | `implement` / `sync-ci` run the script first, stop before any work on non-zero, relay output; neither carries its own prose rules; neither reads, quotes, or substitutes the shipped template | `repo-config: A skill stops before doing any work`; `repo-config: The repo's file is the only runtime source` | ✅ Covered |
| AC | `project-manager` runs the script at session start and offers seeding on failure; the offer is its alone | `repo-config: The coordinator offers to fix it`; `repo-config: The seeding offer belongs to the coordinator alone` | ✅ Covered |
| AC | Seeding in an unconfigured repo → branch + file + PR, zero commits and zero pushes to the default branch | `repo-config: Seeding an unconfigured repo`; `repo-config: The default branch is discovered, never assumed` | ✅ Covered |
| AC | Policy file already on the default branch → no change, no PR, reports the repo already owns its policy | `repo-config: A repo that already owns its policy is left alone` | ✅ Covered |
| AC | Stack detected → concrete command, not the generic five-stack list; unrecognized → says so and names what the owner must fill in, rather than guessing | `repo-config: A proposal is offered whether or not the stack is recognized`; `repo-config: The owner is asked before anything is written` | ✅ Covered — **shape changed by owner decision D7.** The criterion's intent (never write an unconfirmed guess) is met more strongly: seeding confirms every proposal with the owner before writing, so the detected/undetected branch no longer determines whether a guess can land. The generic five-stack list is gone entirely. |
| AC | The seeded file is self-describing about what is safe to edit; reads as the repo's choice, not spec-flow's requirement | `repo-config: The owner reads the file` | ✅ Covered |
| AC | Repo's policy matches the shipped template unchanged → behavior identical to today for a tiered repo | `test-policy: No behavior change for a previously-tiered repo` | ✅ Covered — **only as of `issue-53`.** Marked covered here while no template existed, which left the criterion unverifiable: the scenario does cover it, but there was nothing to compare a repo's policy against. The template now ships at `plugins/spec-flow/references/seed-policy-tiered.md`. |
| AC | `implement` in either mode → both use the same policy text from the same file; the two duplicated copies cannot diverge | `test-policy: Both implement modes use the same line`; `test-policy: A consumer that cannot read the repo refuses rather than defaulting`; `test-policy: Other callers reuse the same generated line` | ✅ Covered |
| AC | A reviewer's report reflects the repo's policy rather than asserting the unit tier unconditionally | `test-policy: Reporting compliance with the policy`; `test-policy: Static agent definitions carry no tier`; `test-policy: The exact commands are named` | ✅ Covered |
| AC | `spec-flow/` is committed, not swallowed by the `.spec-flow/` gitignore entry; docs distinguish the two explicitly | `repo-config: The config directory is committed`; `repo-config: The documentation distinguishes the two directories`; `repo-config: An ignored config directory is diagnosed, not merely reported missing` | ✅ Covered |
| AC (added at activation) | This PR merges → `spec-flow/CI.md` already exists here, so the check passes immediately and no run in `skills` is left refusing | `repo-config: The repo is configured on merge`; `repo-config: The policy states what is actually true here` | ✅ Covered |
| Owner (mid-design) | A repo with no test-running CI is a first-class case, not a degraded one — the driving case across ~20 repos with ~20 sets of rules | `test-policy: A repo with no test suite is a valid policy, not a degraded one`; `test-policy: Running nothing can be full compliance` | ✅ Covered |
| Owner (mid-design) | `sync-ci` must not fail hunting an artifact a no-CI repo never produces | `test-policy: Syncing CI failures where CI is not a test gate`; `test-policy: Syncing CI failures where CI is a test gate` | ✅ Covered |
| Amendment (owner-approved, mid-implementation) | A run summary assembled with no review round having happened must not assert a test outcome it never observed | `test-policy: A report assembled without a review reports that it does not know` | ✅ Covered — found during implementation: the original four-value enum had no way to say "nobody checked", so all four would have been false in the three no-review positions. Fifth value `unknown` added, matching the neighbouring `spec_conformance` field's existing precedent. |
| Defect (found after merge, resolved by `issue-53`) | This change's artifacts asserted a seeding template it never created — `proposal.md:12`, `specs/test-policy/spec.md:120`, `specs/repo-config/spec.md:111`, row 18 above, and `bee37bb`'s own commit message all name one; `tasks.md` section 2 has no task that writes it, and task 2.3 instead directs `setup` to author a policy per repo | Resolved by `issue-53` | ✅ Covered — the template now ships at `plugins/spec-flow/references/seed-policy-tiered.md`, and `specs/repo-config/spec.md:5`'s `MAY` is corrected to `SHALL` above. Found after `bee37bb` merged, so the artifacts asserted the file before it existed; `issue-53` makes them true rather than deleting the claim. |
| Risk | Stale worktrees: every worktree branched before merge refuses until rebased | `repo-config: A missing file inside a stale worktree names the rebase` | ✅ Covered |
| Risk | A skipped read is silent — an agent that never opens the policy has no gate and says nothing | `test-policy: The exact commands are named` | ✅ Covered — contained, not eliminated. An agent that did not read the policy cannot name its commands, so the omission surfaces in the review contract. Full elimination would require verifying the read itself, which no mechanism here provides. |
| Risk | `project-manager` must not offer to seed into a directory that is not a git repo | `repo-config: Run outside a git repository`; `repo-config: The environment error is distinguishable from the missing-config error` | ✅ Covered |
| Risk | An absolute or traversing `SPEC_FLOW_CONFIG_DIR` in checked-in settings is wrong on every other clone | `repo-config: An absolute override is rejected`; `repo-config: A traversing or unsafe override is rejected` | ✅ Covered |
| Risk | The pointer is embedded in a JS template literal; a backtick or `${` in it would break or inject | `test-policy: The generated line is safe to embed`; `repo-config: A traversing or unsafe override is rejected`; `repo-config: A repository root that cannot be described safely is refused` | ✅ Covered — the guarantee is structural, and it has two halves, not one. The only variable in the line is the path; `SPEC_FLOW_CONFIG_DIR` is character-validated against a strict allow-list, and the repo root, which git supplies rather than this script, is guarded separately by both subcommands. See design.md D3a. |
| Amendment (round 3) | A repo root the pointer cannot carry safely stops the pipeline, and an over-wide guard would refuse ordinary checkouts | `repo-config: A repository root that cannot be described safely is refused`; `repo-config: An ordinary repository root is never refused for its punctuation` | ✅ Covered — recorded in design.md D3a. The refusal is an environment error (exit 2), the same class as running outside a git repository, so it does not contradict `Everything present costs nothing`, which governs a repo the pipeline can serve. |
| Amendment (round 3) | A committed symlink can redirect the policy read to a file outside the repo, which every panel agent then loads into context | `repo-config: The policy must physically live inside the repository` | ✅ Covered — recorded in design.md D3b. Reverses the earlier decision to soften the containment wording rather than enforce it. A path check, not a content check: the rule that the check never inspects the policy's text is untouched. |
| Risk | A gitignore entry for the undotted `spec-flow/` would match `plugins/spec-flow/` and erase the plugin from git | `repo-config: The documentation distinguishes the two directories`; `repo-config: An ignored config directory is diagnosed, not merely reported missing` | ⚠️ Partially excluded — the docs warn and the check diagnoses, but nothing *prevents* the edit. A repo can still ignore its own plugin source. Preventing it would require spec-flow policing a consuming repo's `.gitignore`, which is out of scope. |
| Risk | Contract change mid-flight: a run spanning the merge could mix old and new enum values and fail schema validation | — | ⚠️ Excluded — no scenario. Mitigated operationally by merging when nothing is in flight, not by a behavior the spec can assert. |
| Risk | A slow policy is now expressible; a repo can make every TDD cycle slow | — | ⚠️ Excluded by design. This is the repo's choice and the point of the change. Task 2.5 requires the seeded file to state that the local gate runs on every cycle, so the cost is visible when the policy is written. |
| Risk | Workflow mode is structurally weaker — every future config file needs a new `args` field and `SKILL.md` line | — | ⚠️ Excluded — a property of the Workflow tool's sandbox, not of this change. Recorded in `design.md` Risks; the wider fix (extract every shared prompt, or retire Workflow mode) is a deferred separate issue. |
| Debt (fold in) | `docs/workflow.md` states the policy twice; `README.md` twice; both copies must convert together | Tasks 7.1, 7.2 | ✅ Covered — tasks, not scenarios: prose conversion has no runtime behavior to assert. |
| Debt (fold in) | The four non-spec lenses' "leave `tests_ran` as `full`" placeholder | Task 4.3 | ✅ Covered |
| Debt (defer) | Wider `SKILL.md` / `implement.workflow.js` prompt duplication (`GUARDRAILS`, `REVIEW_GUARDRAILS`, panel and fix-loop prompts) | — | ⚠️ Excluded — owner deferred to a separate issue, to be filed after this merges. Not folded into scope. |
| Debt (defer) | Dead `spec_conformance` / `tests_ran` fields on the four non-spec lenses | — | ⚠️ Excluded — owner deferred; restructuring the review contract here would widen the blast radius past what the issue needs. |
| Debt (defer) | `adopt-tiering` bakes in the same split assumption | — | ⚠️ Excluded — explicitly out of scope in the issue; owner deferred filing. |
