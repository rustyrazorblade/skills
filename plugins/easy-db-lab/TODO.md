# easy-db-lab TODO

## easy-db-lab-parallel wrapper

Add an `easy-db-lab-parallel` wrapper to multi-DC cluster workspaces (generated by `setup-cluster.sh`). Runs a command against all DC wrappers in parallel. Needs design decision on output handling — may be able to discard output entirely and rely on easy-db-lab's own logging rather than capturing stdout.

## setup-vpc-peering.sh — VPC ID discovery

Currently the agent reads VPC IDs from `easy-db-lab status` output manually before calling `setup-vpc-peering.sh`. Would be cleaner if easy-db-lab had `easy-db-lab aws vpc-id` and similar subcommands to make this scriptable without parsing status output.
